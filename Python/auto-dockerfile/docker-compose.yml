# web server
nginx:
  image: nginx:latest
  ports:
    - "80:80"
    - "443:443"
  volumes:
    # app
    - /data/www:/usr/share/nginx/html
    # nginx configs
    - /data/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    - /data/nginx/conf.d/:/etc/nginx/conf.d/:ro
    # certificates
    - /data/nginx/ca/server.crt/:/etc/nginx/server.crt:ro
    - /data/nginx/ca/server.key/:/etc/nginx/server.key:ro
  links:
    - fpm:__DOCKER_PHP_FPM__

# php-fpm
fpm:
  build: ./php-fpm
  ports:
    - "9000"
  volumes:
    - /data/www:/usr/share/nginx/html
    # Your php.ini
    - /data/php/conf.d/php.ini:/usr/local/etc/php/php.ini:ro
  # environment:
    # Set your APP env variables here:
    # - APP_KEY=
    # - DB_HOST=
    # - DB_DATABASE=
    # - DB_USERNAME=
    # - DB_PASSWORD=
  links:
    - mysql:mysql

# database
mysql:
  image: mysql:latest
  ports:
    # Allow client to access 3306
    - "3306:3306"
  volumes:
    # NOTE: your data will be stored in ./mysql
    - /data/mysqlData:/var/lib/mysql
  environment:
    - MYSQL_ROOT_PASSWORD=your_mysql_password
